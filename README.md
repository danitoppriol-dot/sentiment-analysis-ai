# sentiment-analysis-ai
This project implements a basic Natural Language Processing (NLP) pipeline to classify text as positive or negative. It uses a Bag-of-Words representation (CountVectorizer) and a Multinomial Naive Bayes model to train and predict sentiment from textual data. 
